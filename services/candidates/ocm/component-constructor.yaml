# ${REGISTRY} and ${VERSION} are substituted at push time by the build script.
components:
  - name: github.com/konfidence-project/example-app/candidates
    version: ${VERSION}
    provider:
      name: konfidence-project
    sources: []
    resources:
      - name: candidates-kustomization
        version: ${VERSION}
        relation: external
        type: kustomize
        access:
          type: ociArtifact
          imageReference: ${REGISTRY}/candidates-kustomization:${VERSION}

      - name: konfidence-manifest
        relation: local
        type: cloud.konfidence.artifact.manifest
        input:
          type: file/v1
          path: ./konfidence-manifest.json

      - name: verify-db-ready-manifest
        version: ${VERSION}
        relation: local
        type: cloud.konfidence.artifact.task.manifest
        input:
          type: file/v1
          path: ./tasks/verify-db-ready/task-manifest.json

      - name: run-schema-migration-manifest
        version: ${VERSION}
        relation: local
        type: cloud.konfidence.artifact.task.manifest
        input:
          type: file/v1
          path: ./tasks/run-schema-migration/task-manifest.json

      - name: verify-migration-manifest
        version: ${VERSION}
        relation: local
        type: cloud.konfidence.artifact.task.manifest
        input:
          type: file/v1
          path: ./tasks/verify-migration/task-manifest.json

      - name: candidates-migrations-image
        version: ${VERSION}
        relation: external
        type: ociImage
        access:
          type: ociArtifact
          imageReference: ${REGISTRY}/candidates-migrations:${VERSION}
