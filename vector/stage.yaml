apiVersion: konfidence.cloud/v1alpha1
kind: Stage
metadata:
  name: example-app
  namespace: example-landscape
spec:
  # The concrete assembled vector, taken from the VectorTemplate's
  # status.latestVector. 04-apply-konfidence-resources.sh substitutes it.
  vector: __VECTOR__
