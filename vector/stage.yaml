# The initial (empty) vector is filled by the VectorPromotionConfig, which
# promotes the VectorTemplate's assembled vector into this Stage. Apply into the
# managed (landscape) namespace.
apiVersion: konfidence.cloud/v1alpha1
kind: Stage
metadata:
  name: example-app
spec:
  vector: ""
